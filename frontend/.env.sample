# The browser (possibly remote via Tailscale) must reach the backend at the
# HOST's address — "localhost" here would mean the client machine, not the host.
# No trailing slash: some components do `VITE_API_PATH + '/api/...'`.
VITE_API_PATH=http://100.79.76.79:5000
